<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomBetterPickerTheme" parent="BetterPickersDialogFragment.Light">
        <item name="bpTextColor">@color/colorPrimary</item>
    </style>

    <style name="TimerSummaryTitleFont" parent="Base.TextAppearance.AppCompat">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="TimerSummaryValuesFont" parent="TimerSummaryTitleFont">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="TimerTitlesFont" parent="Base.TextAppearance.AppCompat">
        <item name="android:textSize">35sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

</resources>
